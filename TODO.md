# TODO

Working backlog for dspi-web-console, grouped by horizon. Shipped work is
recorded in [CHANGELOG.md](./CHANGELOG.md) (generated by release-please), so keep
this file to *upcoming* work. When an item grows beyond a line or needs
discussion, open a GitHub Issue and link it here.

## Now

- [ ] Enable branch protection on `master`: require a PR and a green **CI** check
      before merge (GitHub → Settings → Branches).
- [ ] Enable **Allow GitHub Actions to create and approve pull requests**
      (Settings → Actions → General) so release-please can open its release PR.
- [ ] Integrate the inline channel-rename work (`feat/channel-rail`, currently
      local) — open a PR once ready.

## Next

- [ ] Extract the shared inline-edit lifecycle used by both `ChannelRow` and
      `ChannelNamesPanel` (rename plan deferred this as a follow-up).
- [ ] Sort, merge, and clean up the stale local/remote branches and the
      uncommitted working docs at the repo root.
- [ ] Add a CODEOWNERS file and GitHub issue templates once issue tracking moves
      to GitHub.

## Later

- [ ] Cut `1.0.0` once the wire/firmware contract (V10) is declared stable; until
      then stay on the `0.x` line.
- [ ] Document the supported DSPi firmware range per release in the GitHub
      Release notes (cross-reference `docs/FW-VERSIONS.md`).

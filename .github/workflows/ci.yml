name: CI

on:
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    # Squash-merged PRs (committed by GitHub, title carries "(#NN)") were
    # already verified by the pull_request run; the push trigger only covers
    # direct pushes. The committer check keeps locally-pushed commits that
    # merely mention "(#123)" (e.g. reverts of squash merges) verified.
    if: >-
      github.event_name != 'push' ||
      !(contains(github.event.head_commit.message, '(#') &&
        github.event.head_commit.committer.email == 'noreply@github.com')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci

      - name: Typecheck (svelte-check + tsc)
        run: npm run check

      - name: Lint
        run: npm run lint

      # Disabled for now: `lint:comments` shells out to `uvx`, which the
      # ubuntu-latest runner does not ship, so the step died with exit 127
      # before unwaffle ever ran. Re-enable once the job installs uv
      # (e.g. astral-sh/setup-uv). Run it locally with `npm run lint:comments`.
      # - name: Lint comments
      #   run: npm run lint:comments

      - name: Test (unit + integration, no hardware)
        run: npm test

      - name: Build
        run: npm run build

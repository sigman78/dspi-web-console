# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
.vite
.env*

# Reference material kept locally, not part of this repo.
# DSPi/ is the firmware source, cloned separately (not a submodule).
_base*/
DSPi/

# Local tooling scratch (browser-automation captures, agent workspaces)
.playwright-mcp/
.superpowers/

# Local-only working docs (plans, reviews, ideation) — kept out of the repo by
# convention. Under docs/, everything is local scratch except the canonical set
# whitelisted below; new docs/*.md are ignored by default (git add -f to track).
docs/archived/
docs/ideation/
docs/notes/
docs/plans/
docs/superpowers/
docs/*.md
!docs/ARCH.md
!docs/HW-DSPUSB.md
!docs/HW-NOTIFICATIONS.md
!docs/MULTIDEVICE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# /fw-watch skill — local-only firmware-tracking output (see .claude/skills/fw-watch)
docs/FW-TODO.md
docs/.fw-watch-state.json
